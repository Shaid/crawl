# Black Crypt — Open Work

The single status surface for this game. Every genuinely open item gets one
row here; `plan.md`'s "Open Questions" and `AGENTS.md`'s "Not yet solved"
must not carry item status — they point here. Evidence and paths-tried
tables live in `docs/blackcrypt/amiga/data-structure.md` (or the `dos/`
doc); this file only says what's open and where to read about it.

| ID | Status | Question (one line) | Evidence | Updated |
|----|--------|---------------------|----------|---------|
| blackcrypt-ddraw-wrapper | deferred: visual confirmation needs the project owner's own manual Wine/Windows test, or a screenshot-capable environment | Does dropping a community DirectDraw wrapper DLL (`cnc-ddraw`, MIT) next to `crypt.exe` fix 1C's tiny-letterboxed-viewport presentation bug? Candidate picked and justified over DDrawCompat/dgVoodoo2; the drop-in mechanism itself is confirmed (native `DDRAW.dll` load trace, no crash under Wine with `WINEDLLOVERRIDES="ddraw=n,b"`); the visual pass/fail could not be obtained this session — this sandboxed desktop's X11 capture tools return blank frames (Xwayland-rootless doesn't mirror the real Wayland-composited screen) and the one capture path that does work (KDE `spectacle`) can only do a whole-desktop grab, which this session's desktop being shared with other concurrent agent work made inappropriate to use | `dos/full-game-restoration-plan.md` § "1E. DirectDraw modernization (stretch)" | 2026-08-04 game-re |
| automap-trap-tile | open | Residue from the now-closed `automap-tilemap-source`: automap **tile 14** (framed square, magenta fill = a *visible* trap) is selected by `RevealAroundParty` only when a type-`0x1E` record has `byte +0x07 == 0` **and** `word +0x0E != 0`, but **all 41 trap records (gfx `0x4A`) across all 13 maps ship with `+0x07 = 1`** (= "inviso", confirmed against the clue book's separate `FLOOR PLATE` / `INVISO FLOOR PLATE` legend symbols), so the tile is never drawn from on-disk state. Open: does anything clear `byte +0x07` at runtime (trap detection/disarm)? 11 candidate `byte +0x07` writers exist in S_1; none traced to the `A4 − 0x6E7A` object array | `amiga/data-structure.md` § "bcdfa — UI / Automap Resource Bank" → "The 24 automap tiles" (tile 14 row + the `byte +0x07` inviso table) | 2026-08-02 re-codebreaker |
| bcdfa-eff-spell-map | open | **92 of 95 effects now attributed** — the on-disk hypothesis was right (`bcdfs` type-`0x10` sub-kind-3 word `+0x10` → effects 1–4 rune / 5–8 trigger, 13/13 in range) and a second, larger mechanism was found (`CastSpellRay` S_1 `+0x06D9A`'s third stack word → effects 9–88 across 26 named spells, exact stride-3 lattice, no holes); constants cover 0, 29–37, 89–94. Residue: effects **31, 32, 34** have no identified consumer anywhere, and `bcdfs` type-`0x10` **sub-kind 2**'s `word +0x10` (54/56/57) and `byte +0x07` (7/8/9) on 5 map-2 records are read by neither traced consumer | `amiga/data-structure.md` § "bcdfa — BCSPEED.EFF" → "Which effect belongs to which spell — **SOLVED**"; `bcdfs` § "Structure record field map — type `0x10`" | 2026-08-02 re-codebreaker |
| door-frame-w0c-meaning | open | Residue left by the now-closed `door-cf34-depth0-effect`: word **`+0x0C` of a Door-frame (`0x11`) record is unmapped**. The old "structure-present / occupancy flag" reading was wrong — the `TST.W` at S_1 `+0x112E6` it cited reads the *Door switch* (`0x0F`) record, not the frame. No consumer of the frame's own `+0x0C` has been traced | `amiga/data-structure.md` § "Door State" → "Structure record field map — Door Frame (type `0x11`)" (the struck-through `0x0C` row) and "Consumer 1 (corrected) — the door switch fires the door's action chain" | 2026-08-02 re-codebreaker |
| viewport-kind-handler-bodies | open | **All 14 kind-handler bodies are now traced** (kinds 0-3 `+0x02A0E`, 4/12 `+0x0224C`, 5/6/7, 8/9 `+0x02806`, 10 `+0x02627A` byte-exact, 11, 13) and the `objRec[+4] & 0xF0` switch is **fully mapped** — with a refuted premise: that nibble is the square's **N/E/S/W wall bitmask**, not an object class (`A3 = 1 << (facing+4)` is ANDed against it). Whole-corpus census: 12 distinct values, 11 handled arms all exercised, `0xF0` = monsters intercepted earlier, the 4 unhandled values occur 0 times. 13 per-structure renderers bound to their sprite banks byte-exactly (alcove `$BC` 11/11 incl. both loader mirrors, plaque `$C0` 11/11, stairs `$C4` 14/14 = 2×14,340 = 28,680, pillar `$14`, pits `$10`, buttons `$1C` 18/18, pull chain `$20` 1,330, door lock `$18` = the per-level `bcdfb`-`bcdfn` decor block 3×644, floor items `$30` 49 groups). Residuals: ~~(a) which container fills slot `$00`~~ **SOLVED — see `blackcrypt-floorplate-placement-wiring` below.** Slot `$00` is `bcdfa`'s own UI panel bank RLE stream (`sprites/ui-panel.json`); its 4 "Pressure Plate 1/2 Up/Down" records are byte-exact against the floor-plate descriptors' `src` 18,764/18,820/18,876/18,904. (b) slot `$C8`'s "special panel" body at `+6,060`, past the chunk's declared end; (c) `$51A(A5)`, the door-family `+0x24` position variant, has no traced write site; ~~(d) the kind-3 left-wall position tables~~ **CLOSED — confirmed dead code, with an engine off-by-one as the cause.** `kind == 3` is *structurally* impossible for types `0x22`/`0x0F` from either through-corridor facing: the `+0x032D4` gate forces `partyFacing` to the parity opposite the wall axis and the bit→wall mapping forces `dir` to that same parity, so `kind` is always even. Exhaustive sweep of all 157 records × all 4 facings × all set bits = 504 combinations: kind 0 × 252, kind 2 × 252, kind 1 and kind 3 × **0**; 153/157 corridors are open at both ends, so both facings really are playable. `kind 3` means “wall on the *near* edge” (what the gate rejects); the physically-left wall arrives as `kind 0`, which `+0x02A0E`’s `dir ≥ 2` test discards — so a door lock/switch is only ever drawn on the party’s **right**. Two prior premises corrected: `kind == 2` is not forced (it is an exact 50/50 with kind 0), and `dir ≤ 1` draws *nothing* rather than falling through to the generic renderer | `amiga/data-structure.md` § "3D Viewport Compositing" → "The Phase-1 object switch at S_1 `+0x033D2`", "Kinds 0–3", "Kinds 4 and 12", "Kinds 8 and 9", "Kind 10", "Kind 3 and the left-wall position tables", "Which routine draws which bank", and the remaining rows of "Still open" | 2026-08-02 re-codebreaker |
| blackcrypt-dos-demo-restoration | open | Could the Windows demo (`data/blackcrypt/dosvga/`, 1 of 13 maps) be completed using the already-understood Amiga data? **Phase 1A resolved — GO** (map-switch machinery intact, only a 12-byte call site was removed — `fcn.00426880`/`fcn.00425350`/`fcn.004258d0`, keyed on the runtime offset table at `0x4738b4`). **Phase 2 resolved — DONE:** `scripts/bclib/maindung.py` converts Amiga `bcdfs` to the DOS encoding, verified byte-exact (15,099/15,099 B, zero deviation) against the shipped `maindung.gam`; full 13-map output is 171,005 B. The swap rule turned out itemType-*independent* (a single positional composition, varying only on monster-vs-item), not per-itemType as first framed; the `bcdft` name-reference word needed no remap (`crypt.exe` embeds the identical string block verbatim). **1B resolved — no new item art needed:** found the item-icon resolver (`fcn.00403550`/`fcn.004033c0` off a 235-entry `gfxNumber→iconIndex` table at `0x431b16`) and the key resolver (`fcn.004033d0`, `gfxNumber−200` arithmetic, matching the already-confirmed Amiga rule); all 166 game-wide item/key `gfxNumber`s (42 demo + 124 maps-2-13-only) resolve to real, already-shipped `clipper.clp` art — 139/139 exact match against the independent Amiga LUT, 27/27 keys covered, 0 misses, plus a structural check against the real directory (icon range 0-174 maps exactly onto the 175 real `Start Items` entries). **Phase 4 resolved — DONE:** `scripts/patch_crypt_exe.py` restores the removed map-switch call — a 5-byte `jmp rel32` at `fcn.00423b50` (file+`0x23b50`) to a 22-byte thunk in confirmed-zero `.text` slack at file+`0x2deb3` that forwards `(fromMap, toMap)` into the intact `SwitchMap` driver (`0x426880`). Both edits assembled with `rasm2` (never hand-computed) and verified byte-exact against the real, unmodified `crypt.exe`: round-trip disassembly of both patched regions matches intended instructions exactly, both jump/call targets resolve to the intended addresses, and a full-file diff shows **zero byte differences outside the two intended windows**. Never writes to `data/blackcrypt/dosvga/crypt.exe`; the script refuses in-place patching and self-checks its own written output. Live end-to-end proof (walk map 1's staircase into a converted map 2 under Wine) was attempted but not completed — the patched exe launches without crashing but this environment lacks input-automation tooling (`xdotool`/`ydotool`/`xte`) and a working screenshot tool to navigate blind past §1C's broken/tiny presentation; static byte-exact verification stands as the complete deliverable, and the scratch-dir recipe is documented for a future session with those tools or a `1C` display fix. **1D resolved — art scoping done, exact Phase 3 payload manifest:** the creature-name table lives at `crypt.exe` `0x430898`-`0x431ae0` (26 × 180-byte records; `0x430800` is a separate 152-byte preamble), and reading its own `Start Monsters` strings directly (never done before) gives **25 real creature identities** (26 named groups minus `0xbd` Statue, a structure, not a creature — already inside §1B's structure-gfxNumber bucket), not 24 as first framed — the earlier count missed `Water Lord` (two words, regex mismiss) and conflated the `0x430800` preamble with the table. Records pair 1:1 with the name-string blocks in reverse file order, confirmed on 9 independently-named creatures matching by theme+count and 23/26 total matching by count alone (residual 3 are Amiga-side mirror-view undercounts, already a documented mechanism). New finding: `0xb7`/`0xb8`'s names flip a plausible-sounding earlier guess — `0xb7` (unplaced recolour) is **Possessor**, `0xb8` (placed, `SOUL KEY`) is **Possessor Body**. **23 new creature clusters** (not 19) → **198 new `clipper.clp` entries**, backed by **187 new sprite conversions** (11 entries alias an already-converted sprite under a different facing name — directory-entry aliasing, already a confirmed container mechanism). Group ids **4-9**: all 4 call sites to `fcn.0040b820` traced exhaustively — id `5` = one-time startup preload, id `6` = the title/splash sequence (both real, both UI bundles the demo already ships), id `0` = an internal directory-build pass; **`4`, `7`, `8`, `9` are never requested anywhere in the binary** — dead ids, not an unscoped resource category, no change to Phase 3's scope. Tilesets/ramps: exact counts confirmed against the already-solved Amiga corpus — `bcdfy` **47** sub-images, `bcdfz` **84** (both replacing the earlier `~84`/`~47` approximation), and accent ramps corrected from "4 more" to **3** (ramps 1, 2, 3 — ramp 4 is never selected by any tileset/level per the confirmed dispatch table, so it was never really needed). Total new Phase 3 pixel-art conversions: **318** (187 creature + 131 tileset), well inside the confirmed ~1,184-entry directory headroom. **Phase 5 resolved — DONE (separate from the completion question, an add-on):** traced the demo's pre-title intro-text mechanism (3 sequential modal `DialogBoxParamA` dialogs in `main`, pages 2-3 sharing one dialog template + one text control id, no page-count/pointer table at all — "how many pages" is just how many call sites exist in `.text`) and added a 4th page about this repo's own restoration work, via `scripts/patch_crypt_exe_add_restoration_note.py`: a new small `DlgProc` + `DialogBoxParamA`-call thunk in the unused remainder of Phase 4's own `.text` cave, a new string in `.rdata`'s confirmed-unused, confirmed-unreferenced tail padding, and a single 5-byte hook — byte-exact verified (round-trip disasm of all 3 windows, 0 diff outside them) the same way as Phase 4; composable with `patch_crypt_exe.py` if that one is applied first (its own pre-flight check is stricter than what it writes). **Phase 3a resolved — DONE (proof of concept, not the full injection):** before attempting Phase 3's full 23-cluster/2-tileset injection, proved the Amiga→DOS art-conversion pipeline end to end with a cheap in-place pixel-data swap using only entries the demo already ships — no new directory entries, no offset shifting, no file-size change, because `bcdfz`'s sub-images are byte-identical in (width, height) to the existing map-1 tileset entries at every position, and every Rock Eye replacement frame was explicitly resized to its target slot's existing dimensions. Swapped 68/70 of map 1's tileset entries (indices 7-76) with `bcdfz` (late-game, ramp 2 "bone/cream") art — 58 direct sub-images + 3 `hconcat` wall composites (`Wall 0/1/2`) + 1 `hflip` (`Floor 2`); `Wall Left`/`Wall Right` left as original `bcdfx` pixels (undocumented perspective-composite recipe, out of scope for a POC). Swapped all 7/7 Rock Eye entries with Green Guy art (the only real creature whose frame set — no exact 7-frame match exists anywhere in the roster — reasonably fills Rock Eye's 7 fixed slots without inventing new directory rows). `scripts/build_proof_of_concept_art_swap.py` self-verifies: output file length unchanged (1,151,267 B), full 816-entry directory byte-identical to the input, zero changed bytes outside the 75 touched entries' own data windows, and a visual spot-check confirms real, coherent converted art (no seams at the wall composites, a recognisable creature at each Rock Eye slot) rather than noise that merely satisfies the byte-count check. Live in-game confirmation remains blocked on the same §1C Wine/DirectDraw issue as Phase 4/5. Remaining open: **1C** Wine viability (launches, presentation/DirectDraw scaling broken — narrowed to a display-config problem, and also the blocker for Phase 4's live proof and for Phase 3a's in-game confirmation), **Phase 3** the full resource injection itself (creatures + tilesets only, manifest exact, insertion mechanics documented but not implemented — Phase 3a deliberately only exercised the cheaper same-size-overwrite path) | `dos/full-game-restoration-plan.md` §§ "1A", "1B", "1D" (RESOLVED block: creature table, group-id trace, tileset/ramp counts, full payload manifest), "Phase 2 — DONE", "1C", "Go / no-go checkpoint", "Phase 3a" (DONE block: container mechanics, tileset + Rock Eye swap mappings, full verification table), "Phase 4" (DONE block with full verification table), "Phase 5" (DONE block: display-mechanism trace, slack-space search, patch + verification); `dos/data-structure.md` §§ "crypt.exe", "Record byte-swap is not a blanket word-swap" (full Phase 2 derivation + evidence table); `amiga/data-structure.md` §§ "Monster sprite clustering", "bcdfx / bcdfy / bcdfz", "Dungeon accent-ramp selection" (ground truth this pass cross-referenced against) | 2026-08-04 game-re |
| blackcrypt-floorplate-placement-wiring | open | Art source is now solved (see `data-structure.md`), so the only remaining gap is wiring: floor-plate/trap renders the same 28-byte descriptor at up to 13 ("near") or 11 ("far") **fixed sub-tile grid positions** per visible square — which array index a given plate's on-square position (N/E/S/W/NE/SE/NW/SW/center, the same bit convention items use) maps to was not derived this pass, so `buildViewList` can't select the right one of the 13/11 yet. Lowest priority — geometry, art and the position *tables* are all confirmed; only the index-selection formula is open | `amiga/data-structure.md` § "3D Viewport Compositing" → "Still open (M5)" (Floor-plate/trap row); `scripts/export_dungeon_props.py`'s `read_floorplate` | 2026-08-03 game-re |
| walker-mirror-flag-polarity | deferred: full toggle-state-machine simulation needs action-chain/spell-command schema modelling that doesn't exist yet | **RE question resolved — the correspondence does not hold in general, now demonstrated rather than hypothesised.** Disassembled all 10 remaining `JSR $A4982.l` sites: `+0x02E7E` ("level entry") is **provably dead code** (21/21 real callers to its containing function push argument `#1`, which always skips it — exhaustive static call census); the other 9 are real, reachable, and confirmed to toggle unconditionally w.r.t. bit 29 (teleport/pit/spell-command handlers, one of which even restores the party's position after toggling). Cross-checked against the real `bcdfs` corpus: every one of 35 scripted teleport actions (opcode `0x15`/`0x1E`) across maps 2-5 has both its source and target square outside every bit-29 region (0/35 exceptions) — so the flag is flipped to "wrong" by design on every in-game teleport, not just in a hypothetical edge case. A walker must simulate the real toggle state machine, not shortcut to reading bit 29; not implemented this pass (would need action-chain/spell-command modelling the schema doesn't have yet) — the 100 bit-29 squares (maps 2-5 only) render in the same default state as any other square, an unchanged pre-existing gap | `amiga/data-structure.md` § "3D Viewport Compositing" → "`ViewpointChanged` (S_1 `+0x2492A`)" → "`walker-mirror-flag-polarity` (docs/blackcrypt/TODO.md) — resolved this pass, with a real caveat" | 2026-08-03 game-re |
| blackcrypt-savefile-portability | blocked: needs a real Amiga save file, which needs Amiberry access this session couldn't get explicit user permission for (no `AskUserQuestion` tool available, no other synchronous channel) | Is the Amiga original's save format byte-comparable to the DOS demo's real `char1.dat`, making a real Amiga mid-game save convertible into a working DOS save (Phase 2's precedent for save data)? DOS side is now fully reconstructed from `crypt.exe fcn.00401b80` and cross-validated byte-exact against the real save (its embedded 52-byte map-offset table decodes to the exact `[0, 15047, 0, ...]` this same plan already derived independently). Amiga side: the save serializer (`bcdft` S_1 `+0x1957A`–`+0x19cfe`, file `GAMESAVE:CHARACTERS`) was found and fully traced for the first time — never done in this project before — confirming a 168-byte raw character struct per character (same byte count as DOS's own per-character core block), a 13-slot/52-byte map-offset table, and ~20 named party scalars, but was never run against real data. Verdict so far: real structural overlap, not a blanket transform — a Phase-2-style per-field converter looks buildable but needs an actual Amiga save as an oracle to pin down field order, which this session could not obtain | `dos/full-game-restoration-plan.md` § "Phase 6 — save-file format" | 2026-08-04 game-re |
| monster-boss-names | closed | **Accepted as final, not "awaiting more names."** All 6 named epilogue bosses confirmed — Ogre (= map 1 "Two Head", carries the game's only `EMERALD KEY`), Dragonlich (map 4 `0x50`, summoned by the 3 Idols of Temin), Possessor Demon (map 6 `0xb8`, only `SOUL KEY`), Ram Demon (map 7 `0xb5`) + Ram Lord (map 8 `0xb6`, its 4 carried items are the clue book's list byte-for-byte), Great Waterlord (map 10 `0xbc`, **not** map 9), Medusa (map 11 `0xbe`) — plus Estoroth Paingiver (map 13) and Statue (map 11 `0xbd`, the Statue structure gfx, not a creature). 90/204 sprites named across 11 clusters. The remaining **114 sprites / 15 clusters are ordinary, unnamed monster types** (giant rats, skeletons, etc.) — an exhaustive in-file search already confirmed **no indexed bestiary table exists anywhere in the game's data** (every creature-adjacent string is prose, not a name lookup), and the clue book was already fully mined for its one boss-name list. There is no further real source to check; assigning names to the rest would mean inventing them, which this project doesn't do. Two weak, deliberately-unapplied leads are left on record for the curious (map 2 `0x4f`'s only movement-type-3 "thief"; DOS's `Clawpiller Mask` floor item ↔ map 2 cluster C's caterpillar) but are not planned work | `amiga/data-structure.md` § "Monster sprite clustering (maps 2-13) and name resolution" → "Map ↔ dungeon-level mapping", "Cluster → graphics-ID binding", "Creature names from the Manual & Clue Book", "Bestiary name table search" (confirmed negative) | 2026-08-02 user decision |
