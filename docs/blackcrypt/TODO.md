# Black Crypt — Open Work

The single status surface for this game. Every genuinely open item gets one
row here; `plan.md`'s "Open Questions" and `AGENTS.md`'s "Not yet solved"
must not carry item status — they point here. Evidence and paths-tried
tables live in `docs/blackcrypt/amiga/data-structure.md` (or the `dos/`
doc); this file only says what's open and where to read about it.

| ID | Status | Question (one line) | Evidence | Updated |
|----|--------|---------------------|----------|---------|
| automap-tilemap-source | open | The automap **screen** is now decoded (dual-playfield copper at `bcdft` S_1 `+0x1E792`, 3-plane 82×50 8×8 tilemap + 2-plane 21×10 cell grid, its own 32-word palette at `+0x1E886`, and all four art records out of `bcdfa` entry 5) — but nothing traces **which `bcdfs` dungeon square becomes which of the 24 tiles**, nor what writes the playfield-1 tilemap buffer at `$78(A5)`, nor what screen modes 2/3/4 of `$490(A5)` are (mode 1 = the automap; each mode just repoints one copper list at a different buffer, `$54`/`$58`/`$5C`/`$60(A5)`) | `amiga/data-structure.md` § "bcdfa — UI / Automap Resource Bank" → "The automap screen — a dual-playfield display" | 2026-08-02 re-codebreaker |
| bcdfa-eff-spell-map | open | **92 of 95 effects now attributed** — the on-disk hypothesis was right (`bcdfs` type-`0x10` sub-kind-3 word `+0x10` → effects 1–4 rune / 5–8 trigger, 13/13 in range) and a second, larger mechanism was found (`CastSpellRay` S_1 `+0x06D9A`'s third stack word → effects 9–88 across 26 named spells, exact stride-3 lattice, no holes); constants cover 0, 29–37, 89–94. Residue: effects **31, 32, 34** have no identified consumer anywhere, and `bcdfs` type-`0x10` **sub-kind 2**'s `word +0x10` (54/56/57) and `byte +0x07` (7/8/9) on 5 map-2 records are read by neither traced consumer | `amiga/data-structure.md` § "bcdfa — BCSPEED.EFF" → "Which effect belongs to which spell — **SOLVED**"; `bcdfs` § "Structure record field map — type `0x10`" | 2026-08-02 re-codebreaker |
| movement-target-square-variant-pairs | open | **The briefed questions are both SOLVED** (all 7 special-square sub-kinds confirmed against the official Manual & Clue Book: 2=floor pit, 0=ceiling pit, 3=stairs, **4=spinner**, **9=teleport** — the doc's old 4=teleport/9=spinner labels were indeed swapped, and code 2 was never unreachable). The teleport pair is **also now settled** — `+0x10`=0/`gfx 0x41` = **inviso** teleport, 1/`gfx 0x40` = visible, from the only two "INVISO TELEPORT" mentions in the clue book (Level 24 gives a controlled adjacent A/B pair whose destinations disambiguate). Residual: (a) stairs `+0x10`=2/`gfx 0x43` vs 3/`gfx 0x44` ↔ STAIRS UP vs DOWN is still only a *hypothesis* (destination-level-nibble direction, 27/33 vs 26/35 — no clue-book text distinguishes up from down); ~~(b) the render-side *mechanism* of "inviso"~~ — **SOLVED this pass:** the type-`0x12` render case is in the **kind-4/12 handler `+0x0224C`** (`+0x022BE`), not `DispatchSquareObject`; its `SUBQ.W #1` ladder handles `word +0x10` values 1/2/3 only, so `0` (= `gfx 0x41`) and `4` (= spinner `gfx 0x1E`) draw nothing by omission. `+0x10` is a perfect function of `gfxNumber` across all 13 maps (0↔0x41 ×61, 1↔0x40 ×82, 2↔0x43 ×39, 3↔0x44 ×36, 4↔0x1E ×7) — an independent code-side confirmation of the clue-book labelling. Also narrows (a): sub-kinds 2 and 3 are the **two stairs flights** in slot `$C4` (src 0/9,156/12,468 vs 14,340/23,496/26,808; 2 × 14,340 = 28,680 = the slot size exactly), so `gfx 0x43 ↔ flight A`, `gfx 0x44 ↔ flight B` — rendering the two flights is now the concrete way to settle up-vs-down | `amiga/data-structure.md` § "Party Movement / Facing State Machine" → "Special-square sub-kinds"; § "3D Viewport Compositing" → "Kinds 4 and 12"; stairs up/down hypothesis in § "bcdfx/y/z" door-way flight-pairing note | 2026-08-02 re-codebreaker |
| door-frame-w0c-meaning | open | Residue left by the now-closed `door-cf34-depth0-effect`: word **`+0x0C` of a Door-frame (`0x11`) record is unmapped**. The old "structure-present / occupancy flag" reading was wrong — the `TST.W` at S_1 `+0x112E6` it cited reads the *Door switch* (`0x0F`) record, not the frame. No consumer of the frame's own `+0x0C` has been traced | `amiga/data-structure.md` § "Door State" → "Structure record field map — Door Frame (type `0x11`)" (the struck-through `0x0C` row) and "Consumer 1 (corrected) — the door switch fires the door's action chain" | 2026-08-02 re-codebreaker |
| viewport-kind-handler-bodies | open | **All 14 kind-handler bodies are now traced** (kinds 0-3 `+0x02A0E`, 4/12 `+0x0224C`, 5/6/7, 8/9 `+0x02806`, 10 `+0x02627A` byte-exact, 11, 13) and the `objRec[+4] & 0xF0` switch is **fully mapped** — with a refuted premise: that nibble is the square's **N/E/S/W wall bitmask**, not an object class (`A3 = 1 << (facing+4)` is ANDed against it). Whole-corpus census: 12 distinct values, 11 handled arms all exercised, `0xF0` = monsters intercepted earlier, the 4 unhandled values occur 0 times. 13 per-structure renderers bound to their sprite banks byte-exactly (alcove `$BC` 11/11 incl. both loader mirrors, plaque `$C0` 11/11, stairs `$C4` 14/14 = 2×14,340 = 28,680, pillar `$14`, pits `$10`, buttons `$1C` 18/18, pull chain `$20` 1,330, door lock `$18` = the per-level `bcdfb`-`bcdfn` decor block 3×644, floor items `$30` 49 groups). Residuals: (a) which container fills slot `$00` (floor plates/traps, src 18,764-18,932); (b) slot `$C8`'s "special panel" body at `+6,060`, past the chunk's declared end; (c) `$51A(A5)`, the door-family `+0x24` position variant, has no traced write site; (d) the kind-3 (`+0x25E12`/`+0x26070`) left-wall position tables appear unreachable for types `0x22`/`0x0F` yet are exact mirror pairs of the used ones | `amiga/data-structure.md` § "3D Viewport Compositing" → "The Phase-1 object switch at S_1 `+0x033D2`", "Kinds 0–3", "Kinds 4 and 12", "Kinds 8 and 9", "Kind 10", "Which routine draws which bank", and the four remaining rows of "Still open" | 2026-08-02 re-codebreaker |
| monster-boss-names | open | **All 6 epilogue bosses are now named and confirmed** — Ogre (= map 1 "Two Head", carries the game's only `EMERALD KEY`), Dragonlich (map 4 `0x50`, summoned by the 3 Idols of Temin), Possessor Demon (map 6 `0xb8`, only `SOUL KEY`), Ram Demon (map 7 `0xb5`) + Ram Lord (map 8 `0xb6`, its 4 carried items are the clue book's list byte-for-byte), Great Waterlord (map 10 `0xbc`, **not** map 9), Medusa (map 11 `0xbe`) — plus Estoroth Paingiver (map 13) and Statue (map 11 `0xbd` is the Statue structure gfx, not a creature). Enablers: the **map↔dungeon-level mapping** (13 maps hold 28 levels; 336/338 clue-book cross-refs stay in-map) and the **cluster↔graphics-ID binding** (header `+4/+6/+8` is sprite-store order, corroborated by the 214-byte table). 90/204 sprites named across 11 clusters (was 31/4). Residual: **114 sprites / 15 clusters** the clue book simply never names — no in-file bestiary exists (confirmed negative). Weak unapplied leads: map 2 `0x4f` is the only movement-type-3 "thief"; DOS has a `Clawpiller Mask` floor item ↔ map 2 cluster C's caterpillar | `amiga/data-structure.md` § "Monster sprite clustering (maps 2-13) and name resolution" → "Map ↔ dungeon-level mapping", "Cluster → graphics-ID binding", "Creature names from the Manual & Clue Book", "Still open (naming)", "Paths tried (naming)" | 2026-08-02 re-codebreaker |
