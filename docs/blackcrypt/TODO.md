# Black Crypt — Open Work

The single status surface for this game. Every genuinely open item gets one
row here; `plan.md`'s "Open Questions" and `AGENTS.md`'s "Not yet solved"
must not carry item status — they point here. Evidence and paths-tried
tables live in `docs/blackcrypt/amiga/data-structure.md` (or the `dos/`
doc); this file only says what's open and where to read about it.

| ID | Status | Question (one line) | Evidence | Updated |
|----|--------|---------------------|----------|---------|
| bcdfa-entry5 | open | Decode the rest of container-directory entry 5 (`0x111E1`, 34,340 B) — **the `0x0000` sub-record (13,440 B) is now confirmed as the Spell Book background** (100.000% silhouette vs DOS `clipper.clp` "Spell Book"); `0x6E40` (720 B, 15 icons) is rendered with geometry confirmed by disassembly but purpose unidentified; `0x3480`/`0x62C4`/`0x7110`/`0x7230`/`0x7350`/`0x73A0` (17,720 B) remain open — three DOS same-size candidates (Ram Block, 2 unnamed 32×14s) were tested by pixel silhouette and rejected (chance-level agreement) | `amiga/data-structure.md` § "bcdfa — Remaining Unclassified Ranges" (`0x111E1`–`0x15F8D` — Text/UI Resource Bank, incl. new "`0x0000`–`0x3480` — the Spell Book background" subsection) | 2026-08-01 game-re |
| bcdfa-eff-spell-map | open | Complete the BCSPEED.EFF effect → spell map: 15 of the 31 call sites push a constant effect index (recovered); the rest read field `+0x10` of a runtime object-pool record — **a full write-site census now shows no S_1 instruction sets that field for the relevant object kind**; traced back through its reader's caller (S_1 `+0x4662`) to a dungeon-square structure/trap-object lookup (type `0x10`), meaning the real static table is in the on-disk `bcdfs` structure record layout, not executable code | `amiga/data-structure.md` § "bcdfa — BCSPEED.EFF" → "Which effect belongs to which spell" | 2026-08-01 game-re |
| bcdfo-ui-gaps | open | Classify the remaining ~5.3 KB gap between `bcdfo`'s `chargen_logo` and `sigil_0` — **two more monochrome 8×8 fonts found in its middle third this pass** (73 rendered glyphs at `0x9E30`; 26 glyphs at `0xA250` confirmed 100.000% bit-exact vs DOS `clipper.clp` "CG Font"); the leading ~1,128 B (repeating bar pattern) and trailing ~2,300 B (incl. the still-unconfirmed weak sigil-mask lead on its last 280 B) remain open | `amiga/data-structure.md` § "bcdfo — Character Portraits + UI Elements" (Unaccounted gaps → "Gap 2 — two more fonts found" / Paths tried) | 2026-08-01 game-re |
| tileset-slot0c-tail | open | Identify the 320 B tail after `Door Way 3` in the door chunk (slot `0x0C`) — **content now identified**: it is the same 2-px-per-row doorway-aperture wedge as `doorway-1b`'s mask plane (tail rows 9-22 byte-identical to lintel mask rows 13-26), width re-confirmed at 80 px by the row bytes' own mirror symmetry. Leading explanation is now **exporter slack in a fixed 42,754-byte authoring buffer** (all three tilesets decompress slot `0x0C` to exactly that length while their art ends at exactly 42,434; `bcdfx`/`bcdfy` carry no wedge at all; no DOS counterpart; 0 code references to offset 42,434) — plausible but not proven, so still open | `amiga/data-structure.md` (bcdfx/y/z section, "Still open" → Slot `$0C` tail) | 2026-08-01 re-codebreaker |
| movement-target-square-subtypes | open | `ResolveTargetSquare`'s pit/stairs/teleport/spinner sub-behaviour is now traced to the instruction (`record[+0x10]` discriminator, `MoveParty`'s per-code caller handling, a dedicated stairs level-transition call at `+0x16C62`); blocked-code 7 is **not a text message** — it's a screen-shake blitter effect (retracted premise). Residual: result code **2 appears unreachable** from a type-`0x12` record (a dead comparison overwrites it) and Pit's own code path never reaches it either — its real trigger wasn't found; and new evidence suggests the doc's original 4=teleport/9=spinner labels may be **swapped** (4 never relocates, 9 relocates + plays a distinct sound) but this is flagged as a hypothesis, not confirmed, with no ground-truth oracle available | `amiga/data-structure.md` § "Party Movement / Facing State Machine" → "`ResolveTargetSquare`", its result-code table, and "Paths tried" | 2026-08-01 game-re |
| door-cf34-depth0-effect | open | `+0x0CF34` is now traced far enough to show it's a **generic** structure-toggle/animation-slot dispatcher (explicit cases for types `0x1D`/`0x1E` only) — Door frame (`0x11`) has no explicit case and falls through its shared tail into a runtime prop-animation-slot table + event scheduler, not a "pick Door Way sub-image" call as previously guessed. Whether it has any visible effect for a depth-0 door beyond that shared scaffolding is unresolved. **Premise correction:** its caller S_1 `+0x112FC` is *not* a render site — it sits inside `OpenDoorAtParty` (S_1 `+0x1110A`), on the close-the-door branch, so this item is about the door *interaction* path, not the viewport. (The "which sub-image for open vs closed" question this item originally tracked is **solved** for every depth — see Evidence.) | `amiga/data-structure.md` § "Door State" → "Locked doors — SOLVED" (the `+0x112FC` correction) and "Consumer 1"; byte-exact solve in § "3D Viewport Compositing" → "Kind 11 …" | 2026-08-01 re-codebreaker |
| viewport-kind-handler-bodies | open | The 3D viewport render pipeline is **solved** (`DrawViewport` S_1 `+0x02D46`, its 12-square sight-line walk, `AddRenderItem` `+0x27D7A`, the 14-entry kind jump table at `+0x03570`, `DispatchSquareObject` `+0x025B0`); **kind 11 is now fully solved byte-exact** (door frame + closed-leaf render, 13/13 descriptors verified against the door chunk's offset table). Kind 4/12's target address is corrected (`+0x00224C`, not the previously-cited `+0x0222A4`) and confirmed a real type-`0x05`-keyed dispatch function, but its body (the dispatch table itself) isn't traced; kinds 8/9 (`+0x02806`), 10 (`+0x02627A`) and 0-3 (`+0x02A0E`) remain fully untraced; the object-type switch on `objRec[+4] & 0xF0` at `+0x033D2` is still only partly mapped onto the Structure/Item type tables | `amiga/data-structure.md` § "3D Viewport Compositing" → "Kind 11 …" (solved) and "Still open" (remainder) | 2026-08-01 game-re |
| clipper-misc-bucket | open | DOS `clipper.clp`'s catch-all `misc` bucket, reduced from 233 to **19** entries this pass (`Keys`/`Key Holes`/`Floor Items` marker brackets, previously left unclassified or split across buckets, now routed by index range — `Key Holes` cross-verified against the Amiga wall-decoration bank at >=0.95 greyscale correlation, 29/29, all 3 depths). Residue: 11 x 8x7 inside `Start/End CG Numbers` (no Amiga cross-reference attempted) and 8 unnamed depth-2/3 variants of `Sword`/`Hammer` inside `Start/End Throwing Items` (no Amiga counterpart — the Amiga throwing-items bank is provably closed at 2 weapons, Arrow+Dagger, 0 spare bytes) | `dos/data-structure.md` § "clipper.clp — Resource Archive" (misc-bucket correction) | 2026-08-01 game-re |
